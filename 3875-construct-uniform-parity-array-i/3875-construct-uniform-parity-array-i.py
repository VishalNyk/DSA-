class Solution:
    def uniformArray(self, nums1: list[int]) -> bool:
        #if nums1 contains any odd number, operation 2 will always 
        #get odd result based on the fact that we check every element
        # if odd element ,execute operation 1 and if even , do operation
        # as any odd number where j!=i and even-odd=odd only

        #hence always true

        return True
        